export default {
  // Supported locales for the app
  locales: ["ja", "en"],
  // Default locale
  defaultLocale: "ja",
  // Keep clean URLs; no prefix for default locale
  localePrefix: "as-needed"
};

